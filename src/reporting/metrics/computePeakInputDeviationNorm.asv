function peakInputDeviationNorm = computePeakInputDeviationNorm(u, uStar)
    % COMPUTEPEAKINPUTDEVIATIONNORM Computes largest (by Euclidean norm) deviation of input vector from setpoint.
    %
    % AUTHOR: Cole H. Shaigec
    %
    % INPUTS
    %  u (numTimestamps x 2 double) - simulated input trajectory
    %  uStar (2 x 1 double) - input setpoint
    %
    % OUTPUT
    %  peakInputDeviationNorm (double)

    uTilde = u - uStar.';
    uTildeRowLevelNorms = 




end