function duration = getSimulationDuration()
    % GETSIMULATIONDURATION Returns desired duration of closed-loop simulation.
    %
    % AUTHOR: Cole H. Shaigec
    %
    % OUTPUT
    %  duration (positive integer)
    %
    % NOTES
    % - The pipeline user may hard-code the duration to any positive
    % integer they desire to configure the experiment.

    duration = 1000;
end